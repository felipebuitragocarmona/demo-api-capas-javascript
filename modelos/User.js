class User {
    constructor(id, name, username, email) {
        this.id = id || null;
        this.name = name || "";
        this.username = username || "";
        this.email = email || "";
    }
}